.vscode/**
.remember/**
.claude/**
docs/**
.gitignore
.vscodeignore
test/**
*.log
# Note: node_modules is intentionally NOT ignored — vsce re-includes declared
# dependencies from package.json automatically. Adding node_modules/** here
# would also strip transitive deps.
